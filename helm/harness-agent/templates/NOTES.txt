Your agent "{{ .Release.Name }}" is deployed.

Check status:
  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }}

Port-forward to reach it locally:
  kubectl port-forward svc/{{ include "harness-agent.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}

Then:
  curl http://localhost:{{ .Values.service.port }}/.well-known/agent-card.json
