"""a2a-sdk adapter layer.

This is the only place `a2a` types and classes are imported directly.
Nothing under `harness._internal` is part of the public SDK surface —
developer-facing code never imports from here.
"""
